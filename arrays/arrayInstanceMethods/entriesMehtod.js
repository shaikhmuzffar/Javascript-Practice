let Marvel = ["hulk","iron-man","thor","cpt.america","wanda","hawk-eye"]
const entries = Marvel.entries()
// Fetch all the entries of the same data structure.

for(let entry of entries){
    console.log(entry)
}
console.log(entries)